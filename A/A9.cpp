#include <iostream>
using namespace std;
int main()
{
    char c;
    cin >> c;
    while (c != '.') // cat timp c diferit de .
    {
        cin >> c; // citeste de la tastatura
    }
    return 0;
}